export default function DemoBanner() {
  return (
    <div className="bg-primary text-primary-foreground text-center py-2 px-4 text-xs tracking-[0.2em] uppercase font-body font-medium">
      Demo Website — Demonstrational Mockup Only
    </div>
  );
}